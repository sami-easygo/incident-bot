{
  "name": "kick-incident-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.26.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
